{
  "name": "safewalk",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd safewalk-backend && npm install --production",
    "start": "cd safewalk-backend && node auto_migrate.js && node server.js"
  }
}
